"use client";
// import { useUserQuery } from "@/graphql/generated/graphql";

export default function WithNavbarAndFooterPage() {
  // const { data } = useUserQuery();
  // const user = data?.user;

  // console.log(user);
  return <div>WithNavbarAndFooterPage</div>;
}
